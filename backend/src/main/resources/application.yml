spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_campus?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "999876050900-gk1nakd075hfs2k4omjcu9fjmr867itu.apps.googleusercontent.com"
            client-secret: "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"
            scope: email, profile

app:
  jwt:
    secret: ${JWT_SECRET:smartcampus_super_secret_key_for_jwt_must_be_at_least_256_bits_long_abc123xyz}
    expiration-ms: 86400000  # 24 hours
  frontend-url: ${FRONTEND_URL:http://localhost:5174}
  file:
    upload-dir: ${FILE_UPLOAD_DIR:uploads}

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    com.smartcampus: DEBUG
    org.springframework.security: WARN
